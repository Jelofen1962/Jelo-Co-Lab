{
  "name": "jelofen-co-lab",
  "displayName": "Jelofen's Co-lab",
  "description": "Jelofen's simple Co-Lab self-hosted server + extension (vscode) which is made with Love ;)",
  "version": "1.5.2",
  "publisher": "local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:collab.start",
    "onCommand:collab.setConfig"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "collab.start",
        "title": "Jelo Co-Lab: Start Connection"
      },
      {
        "command": "collab.setConfig",
        "title": "Jelo Co-Lab: Set Configuration"
      },
      {
        "command": "collab.disconnect",
        "title": "Jelo Co-Lab: Disconnect"
      },
      {
        "command": "collab.pushDir",
        "title": "Jelo Co-Lab: Push/Sync Directory"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Jelo Co-Lab",
      "properties": {
        "collab.serverUrl": {
          "type": "string",
          "default": "ws://localhost:3000",
          "description": "WebSocket URL of the Jelo Co-Lab server"
        },
        "collab.workspaceId": {
          "type": "string",
          "default": "demo-workspace",
          "description": "Workspace ID used to group collaborators"
        },
        "collab.userId": {
          "type": "string",
          "default": "user-a",
          "description": "Your user ID displayed to others"
        },
        "collab.token": {
          "type": "string",
          "default": "",
          "description": "Workspace token (shared secret)"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "ws": "^8.20.0"
  }
}
