{
  "name": "jelofen-co-lab",
  "displayName": "Jelofen's Co-lab",
  "description": "Simple collaborative coding",
  "version": "0.0.3",
  "publisher": "local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:collab.start",
    "onCommand:collab.setConfig"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "collab.start",
        "title": "Collab: Start"
      },
      {
        "command": "collab.setConfig",
        "title": "Collab: Set Config"
      }
    ],
    "configuration": {
      "title": "Collab MVP",
      "properties": {
        "collab.serverUrl": {
          "type": "string",
          "default": "http://localhost:3000",
          "description": "Collab server URL"
        },
        "collab.workspaceId": {
          "type": "string",
          "default": "demo-workspace",
          "description": "Shared workspace ID"
        },
        "collab.userId": {
          "type": "string",
          "default": "user-a",
          "description": "Unique user ID"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "ws": "^8.20.0"
  }
}
